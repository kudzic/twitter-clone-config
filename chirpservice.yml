server:
  port: 8082


management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
